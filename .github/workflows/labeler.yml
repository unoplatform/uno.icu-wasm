name: Label automation disabled
on:
  workflow_dispatch:

jobs:
  triage:
    # Deliberately inert: branch/PR publication must not mutate labels.
    # Re-enabling automation is a separate owner-reviewed policy change.
    permissions:
      contents: read
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic label mutation is disabled."
